# Kaizen Analysis - 2026-02-26

## Current Situation
- The "Golden Rules" audit (`dotnet run -- --golden-rules`) reports multiple entities with unsynchronized seeds and tests.
- **Affected Entities:**
  - `Tariff`
  - `TariffItem`
  - `PurchaseInvoice`
  - `SalesInvoice`
  - `PurchaseDeliveryNote`
  - `SalesDeliveryNote`
- **Error:** `Seeds no sincronizados (requiere revisión manual)` and `Tests no sincronizados`.

## Impact
- **Data Integrity:** Lack of seed data means developers and QA cannot verify pricing and billing flows easily.
- **Test Coverage:** Missing test data prevents writing comprehensive integration tests for these core financial entities.
- **Compliance:** Violates the "Golden Rules" which require every entity to have corresponding seeds and tests.

## Proposed Action (Kaizen)
- Implement seeding logic in `JsonDataSeeder.cs` for all missing entities.
- Add sample data to `demo-data.json` and `test-data.json`.
- Verify compliance by re-running the Golden Rules check.

## Action Plan
1. Implement `Tariff` and `TariffItem` seeding.
2. Implement `PurchaseInvoice` and `SalesInvoice` seeding.
3. Implement `PurchaseDeliveryNote` and `SalesDeliveryNote` seeding.
4. Verify all entities pass the Golden Rules check.
